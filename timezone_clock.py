\"\"\"Digital Clock with Multiple Timezone Support\"\"\"\n\nimport tkinter as tk\nfrom tkinter import font\nfrom datetime import datetime\nimport pytz\nfrom typing import List, Tuple\n\nclass TimezoneDigitalClock:\n    \"\"\"A digital clock that displays time in multiple timezones.\"\"\"\n    \n    def __init__(self, root: tk.Tk, timezones: List[str] = None):\n        \"\"\"Initialize the timezone clock.\n        \n        Args:\n            root: Tkinter root window\n            timezones: List of timezone strings (e.g., ['UTC', 'US/Eastern', 'Asia/Kolkata'])\n        \"\"\"\n        self.root = root\n        self.root.title(\"Digital Clock - Multiple Timezones\")\n        self.root.geometry(\"800x400\")\n        self.root.configure(bg=\"#1a1a1a\")\n        \n        # Default timezones if none provided\n        self.timezones = timezones or [\n            'UTC',\n            'US/Eastern',\n            'US/Central',\n            'US/Pacific',\n            'Europe/London',\n            'Europe/Paris',\n            'Asia/Tokyo',\n            'Asia/Kolkata',\n            'Australia/Sydney',\n            'America/Toronto'\n        ]\n        \n        # Configure fonts\n        self.title_font = font.Font(family=\"Helvetica\", size=20, weight=\"bold\")\n        self.clock_font = font.Font(family=\"Courier\", size=28, weight=\"bold\")\n        self.label_font = font.Font(family=\"Helvetica\", size=12)\n        \n        # Create main frame\n        self.main_frame = tk.Frame(root, bg=\"#1a1a1a\")\n        self.main_frame.pack(fill=tk.BOTH, expand=True, padx=10, pady=10)\n        \n        # Title\n        title = tk.Label(\n            self.main_frame,\n            text=\"🌍 Digital Clock - Multiple Timezones\",\n            font=self.title_font,\n            fg=\"#00ff00\",\n            bg=\"#1a1a1a\"\n        )\n        title.pack(pady=10)\n        \n        # Create clock display frame\n        self.clocks_frame = tk.Frame(self.main_frame, bg=\"#1a1a1a\")\n        self.clocks_frame.pack(fill=tk.BOTH, expand=True)\n        \n        # Dictionary to store clock labels\n        self.clock_labels = {}\n        \n        # Create clock displays\n        self.create_clocks()\n        \n        # Start clock update\n        self.update_clocks()\n    \n    def create_clocks(self):\n        \"\"\"Create clock displays for each timezone.\"\"\"\n        # Create grid for clocks (2 columns)\n        cols = 2\n        for idx, tz in enumerate(self.timezones):\n            row = idx // cols\n            col = idx % cols\n            \n            # Create frame for this timezone\n            tz_frame = tk.Frame(\n                self.clocks_frame,\n                bg=\"#2a2a2a\",\n                relief=tk.RAISED,\n                bd=2\n            )\n            tz_frame.grid(\n                row=row,\n                column=col,\n                padx=5,\n                pady=5,\n                sticky=\"nsew\"\n            )\n            \n            # Configure grid weights\n            self.clocks_frame.grid_rowconfigure(row, weight=1)\n            self.clocks_frame.grid_columnconfigure(col, weight=1)\n            \n            # Timezone label\n            tz_label = tk.Label(\n                tz_frame,\n                text=tz,\n                font=self.label_font,\n                fg=\"#00ccff\",\n                bg=\"#2a2a2a\"\n            )\n            tz_label.pack(pady=5)\n            \n            # Time display\n            time_label = tk.Label(\n                tz_frame,\n                text=\"--:--:--\",\n                font=self.clock_font,\n                fg=\"#00ff00\",\n                bg=\"#1a1a1a\"\n            )\n            time_label.pack(pady=10, fill=tk.BOTH, expand=True)\n            \n            # Date display\n            date_label = tk.Label(\n                tz_frame,\n                text=\"Loading...\",\n                font=self.label_font,\n                fg=\"#ffff00\",\n                bg=\"#2a2a2a\"\n            )\n            date_label.pack(pady=5)\n            \n            # Store labels for this timezone\n            self.clock_labels[tz] = {\n                'time': time_label,\n                'date': date_label\n            }\n    \n    def update_clocks(self):\n        \"\"\"Update all clock displays with current time.\"\"\"\n        for tz_name in self.timezones:\n            try:\n                # Get timezone\n                tz = pytz.timezone(tz_name)\n                \n                # Get current time in timezone\n                now = datetime.now(tz)\n                \n                # Format time and date\n                time_str = now.strftime(\"%H:%M:%S\")\n                date_str = now.strftime(\"%a, %b %d, %Y\")\n                \n                # Update labels\n                self.clock_labels[tz_name]['time'].config(text=time_str)\n                self.clock_labels[tz_name]['date'].config(text=date_str)\n                \n            except Exception as e:\n                self.clock_labels[tz_name]['time'].config(text=\"ERROR\")\n                self.clock_labels[tz_name]['date'].config(text=f\"Invalid TZ: {tz_name}\")\n        \n        # Schedule next update (every 1000ms = 1 second)\n        self.root.after(1000, self.update_clocks)\n\n\ndef main():\n    \"\"\"Main function to run the timezone clock application.\"\"\"\n    root = tk.Tk()\n    \n    # Custom timezone list (you can modify this)\n    custom_timezones = [\n        'UTC',\n        'US/Eastern',\n        'US/Central',\n        'US/Pacific',\n        'Europe/London',\n        'Europe/Paris',\n        'Asia/Tokyo',\n        'Asia/Kolkata',\n        'Australia/Sydney',\n        'America/Toronto'\n    ]\n    \n    app = TimezoneDigitalClock(root, timezones=custom_timezones)\n    root.mainloop()\n\n\nif __name__ == \"__main__\":\n    main()\n"